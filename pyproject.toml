[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "secu"
version = "1.3.0"
description = "Argon2id deterministic password generator"
requires-python = ">=3.10"
dependencies = [
    "argon2-cffi",
    "pillow",
    "pyperclip"
]
